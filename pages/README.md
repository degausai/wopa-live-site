# pages/

Each participant in the WOPA Session 1 workshop adds a bio page here. They're added via PR by the submit endpoint, then merged on stage by the presenter.

If you're a participant: ignore this folder. Just run `./submit.sh "Your Name"` from the repo root after generating your page with Claude. The script handles the rest.
